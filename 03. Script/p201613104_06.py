# -*- coding: utf-8 -*-
"""프로그래밍 6주차 실습 과제(Jupyter notebook에서 .py 확장자 변환)

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/10lF_ubs3HZ9PgI8Kd62l8ug17UuG-bYb
"""





'''
# 1.1번
# 20kg이 넘어가면 수수료 20000원을 내야 한다. 
# 20kg 미만이면 수수료는 없다
# 사용자로부터 짐의 무게를 표준입력 받고, 사용자가 지불할 금액을 출력한다.

weight = int(input("짐의 무게는 얼마 입니까? "))

if weight > 20 :  # 짐의 무게가 20kg 넘으면
    print("20kg 넘으므로 20,000원을 내셔야 합니다.\n감사합니다.")
else:                # 20kg 넘지 않으면
    print("짐에 대한 수수료가 없습니다.\n감사합니다.")
'''






'''
# 1.2번
# 짐의 개수를 사용자에게 표준입력으로 묻는다.
# 짐의 개수가 2개 이상이면 30000원의 수수료를 더 받는다.
# 하나라도 짐의 무게가 20kg 초과하면 20000원을 더 내야 한다.
# 총 수수료를 계산하면 출력한다.


# 표준입력으로 짐의 무게와 개수를 입력받음
weight = int(input("짐의 무게는 얼마 입니까? "))
number = int(input("짐의 개수는 몇개 입니까? "))


if number >= 2:  # 짐이 2개 이상이면, 수수료 최소 3만원
    
    if weight > 20:  # 무게가 20kg 초과하면(문제에서는 초과라고 함)
        print("20kg 넘고 짐이 2개 이상이므로 50,000원을 내셔야 합니다.\n감사합니다.")
    
    else:  # 무게는 20kg보다 작으면
        print("20kg를 넘지 않고 짐이 2개 이상이므로 30,000원을 내셔야 합니다.\n감사합니다.")

else: # 짐이 1개이면
    if weight > 20: # 무게가 20kg 넘음, 수수료 2만원
        print("20kg 넘고 짐이 1개이므로 20,000원을 내셔야 합니다.\n감사합니다. ")
    else:
        print("짐에 대한 수수료가 없습니다.\n감사합니다. ")
'''






'''
# 2.1번
# id와 password를 표준입력으로 받는다.
# id 길이가 10 넘으면 "회원가입 실패 : id 길이가 10 초과"
# password 길이가 10넘으면 "회원가입 실패 : password의 길이가 10을 초과" 출력한다. 


# 주의 : 파이썬에는 이미 id함수가 있으므로 변수명을 id로 하면 안된다.

ID = input("아이디를 입력하세요 : ")
pw = input("password를 입력하세요 : ")


# len함수 이용하여 길이를 알 수 있다.

if len(ID) >= 10 : # 아이디의 길이가 10 이상이면
    print("회원가입실패 : id 길이가 10초과") # 실제로는 10 이상이 맞다.
    
if len(pw) > 10 :
    print("회원가입실패 : password 길이가 10초과")
    
if len(ID) < 10 and len(pw) < 10 :
    print("회원가입성공")
'''






'''
# 2.2번
# 위 문제에서 ID는 abc, password는 123으로 회원가입했다고 가정.
# 로그인하기 위해 아이디와 비번을 표준입력으로 받는다.
# 아이디가 잘못되면 로그인 실패
# 비밀번호가 잘못되도 로그인 실패
# 아이디와 비밀번호가 모두 맞으면 로그인 성공! 출력

user = {'abc' : 123} # 회원가입 아이디와 비밀번호


# 아이디와 비밀번호를 표준입력으로 받는다.
ID = input("아이디를 입력하시오 : ") # 아이디는 문자열
pw = int(input("비번을 입력하시오 : ")) # 비번은 정수(숫자)


if ID == 'abc' :  # 아이디가 일치하면
    if pw == 123 : # 비밀 번호도 일치하는 경우
        print("로그인 성공!")
        
    else:  # 비밀 번호는 틀린 경우
        print("로그인 실패 : password 오류")
        
else:  # 아이디가 틀림
    if pw == 123:  # 비밀번호는 맞는 경우
        print("로그인 실패 : id 오류")
        
    else: # 비밀번호도 틀린 경우 <--- 이 경우는? 
        # 문제에서는 출력예시가 나오지 않았지만, 굳이 코딩한다면 아래와 같다.
        print("로그인 실패 : id오류, password 오류")
'''






'''
# 2.3번 
# 로그인 하기 위해 아이디와 패스워드를 표준입력으로 받는다.
# 성공했을 때는 로그인성공 출력
# 아이디가 잘못되면 아이디 오류 출력
# 비번이 잘못되면 비밀번호 오류 출력

# 위에서 가입한 사이트에서의 ID와 password 정보를 리스트로 표현한 것
id_list = ['김철수', '홍길동', '김영희']
pw_list = [123, 456, 789]

# 사용자정보(info)를 zip()함수, dict()함수 이용하여 딕셔너리로 바꾼다.
info = dict(zip(id_list, pw_list))


# 아이디와 비밀번호를 표준입력으로 받는다.
ID = input("아이디를 입력하시오 : ")
pw = int(input("비번을 입력하시오 : "))


if ID in id_list:   # 입력아이디가 id_list에 있다면
    if info[ID] == pw : # 아이디와 비밀번호가 일치하면
        print("로그인 성공!")
    else: # 일치하지 않을 때(아이디는 맞고, 패스워드 틀림) 
        print("로그인 실패 : password 오류")
        
else: # 잘못된 아이디 입력
    print("알 수 없는 사용자 입니다.")
'''






'''
# 3번.
# 덧셈, 뺄셈, 곱셈, 나눗셈 수행 프로그램 작성하려 한다.
# 원하는 연산 번호를 입력받고, 2개의 양의 정수 입력받으면 연산 결과 출력해준다.
# 각 연산에 배정된 숫자가 아닌 수를 입력하면 --> 잘못 입력했다는 정보 출력
# 음의 정수를 입력하면 --> "양의 정수를 입력해야 합니다." 라고 출력하고 종료


# 연산과 대응되는 번호
print("1.덧셈 2.뺄셈 3.곱셈 4.나눗셈")

# 연산 번호 입력 받는다.
number = int(input("어떤 연산을 원하는지 번호를 입력하세요 : "))


if 0 < number < 5 :   # number가 1, 2, 3, 4 중 하나이면

    # 2개의 수를 동시에 입력받는 형식, 공백으로 구분 
    int1, int2 = map(int, input("연산을 원하는 숫자 2개를 입력하세요(양의 정수만 가능) : ").split())
   
    if int1 < 0 or int2 < 0:    # 만약 입력받은 수가 음수이면
        print("양의 정수를 입력하세요.")
        
    else: # 양의 정수 2개를 제대로 입력했다면
        
        if number == 1:   # 덧셈 
             print(str(int1) + ' + ' + str(int2) + ' = ' + str(int1 + int2))
    
        elif number == 2: # 뺄셈
            print(str(int1) + ' - ' + str(int2) + ' = ' + str(int1 - int2))
    
        elif number == 3: # 곱셈
            print(str(int1) + ' * ' + str(int2) + ' = ' + str(int1 * int2))
    
        elif number == 4: # 나눗셈, 양의 정수만 입력받으므로 분모가 0이 되는것은 고려할 필요가 없다.
            print(str(int1) + ' / ' + str(int2) + ' = ' + str(int1 / int2))
    
else:  # 연산 번호를 잘못 입력한 경우
    print("잘못 입력하였습니다.")
'''






'''
# 4번.

# 사용자로부터 신장과 체중을 표준입력으로 받는다.
# BMI 값을 계산하고, 
# BMI 조건에 따라 if문을 작성하여 비만도 판정을 출력한다.


# 표준입력으로 신장과 체중 입력받음
weight = int(input("무게(킬로그램) : ")) # kg단위의 정수형으로 받는다.
height = float(input("키(미터) : ")) # 미터단위이므로 키는 float 처리해야 한다.


# BMI 계산
BMI = weight / (height * height)
print("당신의 BMI: ", BMI)


# 비만도 판정
if 20 < BMI < 24.9:
    print("정상입니다.")

elif 25 < BMI < 29.9:
    print("과체중입니다. ")
    
elif BMI >= 30: # 정확히 경우를 나누려면 여기서는, else가 아닌 elif가 적절하다.
    print("비만입니다. ")
'''





